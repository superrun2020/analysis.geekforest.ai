import test from 'node:test';import assert from 'node:assert/strict';import {createServer} from 'vite';import React from 'react';import {renderToStaticMarkup} from 'react-dom/server';
test('daily overall renders one live configuration card and table before authentication/data',async()=>{const vite=await createServer({optimizeDeps:{noDiscovery:true,include:[]},server:{middlewareMode:true},appType:'custom'});try{const {DailyAnomalies}=await vite.ssrLoadModule('/app/daily-anomalies.jsx');const html=renderToStaticMarkup(React.createElement(DailyAnomalies,{ready:false}));assert.equal((html.match(/matrix-overall-config/g)||[]).length,1);for(const label of ['异常原因','错误原因','统计字段','导出 CSV','查询','重置','overall-result-card','显示-应用','显示-包名','显示-昨日率','显示-相对环比(%)','版本筛选','错误消息关键词'])assert.ok(html.replaceAll('查 询','查询').replaceAll('重 置','重置').includes(label),label);assert.ok(html.includes('<thead'));}finally{await vite.close()}});
